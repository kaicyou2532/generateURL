services:
  api:
    build: .
    image: generateurl-api:latest
    ports:
      - "8000:8000"
    environment:
      - BASE_URL=http://133.2.37.149/ig
      - PORT=8000
      - UPLOAD_DIR=/app/uploads
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
