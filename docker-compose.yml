services:
  api:
    build: .
    image: generateurl-api:latest
    ports:
      - "8080:8080"
    environment:
      - BASE_URL=http://localhost:8080
      - PORT=8080
      - UPLOAD_DIR=/data/uploads
    volumes:
      - ./uploads:/data/uploads
    restart: unless-stopped
